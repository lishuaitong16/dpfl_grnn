# GRNN 论文核心要点整理

> 论文：Ren, Deng, Xie. *GRNN: Generative Regression Neural Network — A Data Leakage Attack for Federated Learning*. ACM TIST, Vol.13, No.4, Article 65, May 2022.

这份整理面向"看懂攻击原理 + 写实验报告引用"，分原理、方法、实验、防御四块。

---

## 一句话概括

在联邦学习里，客户端上传的**梯度本以为是安全的**，但 GRNN 证明：恶意服务器仅凭一个客户端上传的梯度 + 公开的全局模型，就能**完整还原出该客户端的私有训练图像和标签**，且不需要任何额外信息（如类别标签），也不要求全局模型收敛。

---

## 二、研究背景与动机

联邦学习（FL）的初衷是"数据不出本地，只共享梯度"来保护隐私。但梯度并非真正脱敏：

- 早期工作发现梯度能泄露数据集的某些属性（property inference）。
- **基于 GAN 的攻击**（Hitaj et al.）能生成与训练集分布相近的图，但需要**已知类别标签**，且与训练数据无显式对应，对数据方差敏感。
- **DLG（Deep Leakage from Gradients）**首次把数据还原建成回归问题：随机初始化"假图+假标签"，在全局模型上算假梯度，最小化假梯度与真梯度的 MSE 距离来还原。但**不稳定**，对 batch 大小和图像分辨率敏感。
- **iDLG** 改进了 DLG 的标签推断（发现真实标签对应的梯度分量落在 [−1,0]），但**只支持 batch=1**。
- **IG（Inverting Gradient）**用余弦相似度损失提升稳定性，能还原高分辨率（224×224）、大 batch（100），但**成功率偏低**。

GRNN 要解决三大未被很好解决的挑战：**模型稳定性、大 batch 下的可还原性、高分辨率的保真度**。

### GRNN 与相关工作对比（论文 Table 1）

| 方法 | 还原模式 | 支持 Batch | 分辨率 | 损失函数 |
|------|----------|-----------|--------|----------|
| DLG  | 判别式   | 小，8     | 低 64×64 | MSE |
| iDLG | 判别式   | 仅 1      | 低 64×64 | MSE |
| IG   | 判别式   | 中，100   | 高 224×224 | 余弦距离 + TVLoss |
| **GRNN** | **生成式** | **大，256** | **高 256×256** | **MSE + WD + TVLoss** |

GRNN 的关键差异：用**生成式**（训练一个生成网络）而非直接优化像素，从而更稳、能扛大 batch。

---

## 三、威胁模型与攻击目标

- **攻击者**：联邦学习里的**恶意（中心）服务器**。
- **攻击者拥有**：全局模型的结构和参数（本来就由服务器下发）、某客户端上传的**真实梯度 g**。
- **攻击者没有**：客户端的私有图像和标签。
- **攻击目标**：仅凭 g 和全局模型，反推出该客户端用于计算 g 的**私有训练图像 + 对应标签**。
- **关键假设**：服务器能拿到**单个客户端未经聚合的梯度**（朴素 FL 中梯度逐个上传即满足）。若有安全聚合（只看到求和结果），攻击难度大增。

---

## 四、联邦学习设定（论文 Section 3.1）

- 采用经典 **FedAvg**：多客户端并行跑若干步 SGD，中心服务器周期性地对模型更新求平均。
- 全局模型聚合：`ω' = (1/N) Σ ω_i`，其中 ω' 为全局模型梯度/参数，ω_i 为第 i 个本地模型，N 为客户端数。
- 服务器先聚合各客户端梯度 `g_i`，再更新全局模型权重：`θ_{t+1} = θ_t − (1/C) Σ g_i`。
- 本作业 backbone 用 **LeNet**（论文还用了 ResNet-18）。

---

## 五、GRNN 方法（论文 Section 3.2，核心）

### 5.1 整体思想

把数据还原**从"高维拟合问题"改造成"数据生成问题"**：用一个 GAN 式生成器当图像生成器，用一个 FC 层当标签生成器，让生成数据算出的"假梯度"去逼近真梯度。通过联合优化两支生成器，使 GAN 的隐空间与全局模型的梯度空间对齐，从而稳定地生成高保真训练数据。

### 5.2 生成器结构（论文 Figure 2 + Table 2）

输入：从**单位高斯分布 N(0,1)** 采样的随机向量 v（每个样本一个，整个攻击过程固定）。生成器 G 有**两支，共享同一输入**：

**① 图像支（fake-data generator）**——遵循 iWGAN 的"由粗到细"设计：
- v 先经一个**分数步长卷积层（FSConv）**产生 4×4 特征图；
- 经过若干**上采样块（Upsampling Block）**逐步提升分辨率；
- 上采样块数由目标分辨率决定，例如 32×32 用三个块（4→8→16→32）。

**上采样块构成（Table 2）**：
| 层 | 设置 |
|----|------|
| 上采样层 | scale factor=2，mode=nearest（最近邻插值） |
| 卷积层 | kernel=3, stride=1, padding=1 |
| 批归一化 | — |
| **GLU（门控线性单元）** | 激活函数 |

> 论文说明：用 **GLU** 而非 ReLU/Sigmoid，因为 GLU 比 ReLU 稳定得多、比 Sigmoid 学得快。该方法对 ReLU 等其他激活也适用。注意 GLU 会让通道数减半。

**② 标签支（fake-label generator）**：
- 一个 **FC 层 + softmax**，输入同一个随机向量 v，输出对应的 fake label。
- 假设输入向量元素彼此独立、服从标准高斯。标签集合与 FL 系统一致。

形式化：`(x̂, ŷ) = G(v | θ̂)`，θ̂ 为 GRNN 可训练参数。

### 5.3 损失函数（论文 Equation 4）

生成 (假图 x̂, 假标签 ŷ) 后，在**当前全局模型**上跑一次前向+反向得到**假梯度 ĝ**。目标是让 ĝ 逼近真梯度 g：

```
L̂(g, ĝ, x̂) = MSE(g, ĝ) + WD(g, ĝ) + α · TVLoss(x̂)
```

- **MSE**：真假梯度向量（拉平所有参数梯度）的欧氏距离平方。
- **WD（Wasserstein Distance）**：最小化两个梯度向量的几何（分布）差异。与 MSE **等权**。
- **TVLoss（Total Variation）**：对生成图像施加平滑约束。**α** 是平滑正则权重。
- 两支生成器都是完全可微的神经网络，**端到端联合训练**。

### 5.4 攻击算法（论文 Algorithm 1）

```
1: g ← 在本地客户端用真实 (x,y) 算出真梯度（被截获）
2: v ← 从 N(0,1) 采样，作为 GRNN 的固定输入
3: for 每次迭代 i ∈ [1..I] do
4:     (x̂_i, ŷ_i) ← G(v | θ̂_i)            # 生成假图、假标签
5:     ĝ_i ← 在共享全局模型上用 (x̂_i, ŷ_i) 算假梯度
6:     D_i ← L̂(g, ĝ_i, x̂_i)               # 算 GRNN 损失
7:     θ̂_{i+1} ← θ̂_i − η(∂D_i / ∂θ̂_i)     # 只更新 GRNN（生成器）参数
8: end for
9: return (x̂_I, ŷ_I)                       # 返回还原的图像和标签
```

**核心：被优化的是生成器 G，全局模型权重始终冻结。** 收敛后 G 的输出即还原结果。

---

## 六、实验设置（论文 Section 4.1–4.2）

- **数据集**：MNIST（7000 张 28×28 灰度手写数字）、CIFAR-100、LFW、VGG-Face。
- **Backbone**：LeNet、ResNet-18，均用 **PyTorch** 实现，代码开源。
- **二阶可导处理**：把所有 **ReLU 换成 Sigmoid**，保证模型二阶可导（计算 MSE 损失对梯度的梯度时需要），与 DLG/iDLG 一致。
- **GRNN 优化器**：**RMSprop，学习率 0.0001，动量 0.99**。
- **TVLoss 权重 α**：LeNet 用 **1e-3**，ResNet-18 用 **1e-6**。
- **攻击时机两种**：
  - **未收敛（non-converged）**：全局模型仅训练第 1 轮（甚至随机初始化）即攻击；
  - **收敛（converged）**：全局模型 loss 变化 δG ≤ 1e-4 后再攻击。

---

## 七、关键实验结论

1. **不需要模型收敛**：真梯度只在全局模型第 1 轮就能拿来还原，说明梯度泄露与模型是否收敛无关。
2. **未收敛反而更好攻**：收敛模型还原效果更差，因为收敛后大量梯度接近 0，携带的输入信息变少。
   - PSNR 对比（batch=1，LeNet on MNIST）：未收敛 **52.37 dB** vs 收敛 **39.47 dB**（越高越相似）。
3. **抗大 batch 能力远超 DLG**：
   - MNIST + LeNet：DLG 在 batch > 8 就失败；**GRNN 能还原到 batch=256**。
   - CIFAR-100、LFW：DLG 只在 batch=1 有效；GRNN 分别能到 batch=64、128。
4. **还原方式更自然**：DLG 是逐像素贪婪还原；GRNN 先在粗尺度对齐整体分布，再在细尺度逐步补充细节（由粗到细）。
5. **质量评价**：用 **PSNR**（峰值信噪比，`PSNR = 10·lg(255² / MSE)`）衡量还原图与真图的相似度，越高越像。
6. 还做了**人脸再识别**实验，GRNN 还原图的 Top-1/3/5 识别准确率均优于 DLG。

---

## 八、防御策略（论文讨论，本作业核心相关）

论文讨论了几类防御，并**重点定量评估了"噪声添加（noise addition）"**：

- **加密类**：同态加密（HE）加密梯度后聚合——开销大，且加密格式下视觉一致性差，模型泛化弱。
- **噪声添加（与差分隐私直接相关）**：往共享梯度上加噪声来干扰还原。
- 论文结论（值得报告引用）：**即使在梯度上加了较高水平的噪声，GRNN 仍能较稳定、成功地还原数据**——说明 GRNN 鲁棒性强，单纯加噪要挡住它需要较大的噪声量（也就意味着较大的精度代价）。这正是本作业要量化的"隐私-效用权衡"。

---

## 九、对你做实验的启示（结合作业）

1. **复现攻击**：照 Algorithm 1，用 Sigmoid 版 LeNet、RMSprop(lr=1e-4, momentum=0.99)、α=1e-3、batch=1 起步，先在未收敛/随机初始化模型上跑最容易成功。
2. **攻击时机对比**：可复现"未收敛 vs 收敛"的还原质量差异（PSNR），印证论文结论。
3. **batch 影响**：从 batch=1 加到 4/8 观察还原质量下降，对应论文的 batch 消融。
4. **差分隐私防御**：在真梯度上做"裁剪 + 拉普拉斯/高斯加噪"，扫不同隐私预算 ε，观察还原图随噪声增大而变糊、PSNR 下降；并结合 FL 精度曲线讨论隐私-效用权衡。论文已指出 GRNN 较鲁棒，所以你的实验大概率会看到"要明显挡住 GRNN，需要较小的 ε / 较大的噪声，此时 FL 精度也明显下降"。

---

## 名词速查

- **FL / FedAvg**：联邦学习 / 联邦平均算法。
- **DLG / iDLG / IG**：GRNN 之前的三种梯度泄露攻击。
- **GLU**：门控线性单元，`GLU(a,b)=a·sigmoid(b)`，通道减半。
- **WD**：Wasserstein 距离，衡量分布差异。
- **TVLoss**：总变差损失，鼓励图像平滑。
- **PSNR**：峰值信噪比，图像相似度指标，越高越像。
- **non-converged / converged**：全局模型未收敛 / 已收敛两种攻击时机。
