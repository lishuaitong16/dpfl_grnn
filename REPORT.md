# 差分隐私联邦学习与 GRNN 梯度反演攻击实验报告

---

## 一、实验目的

本实验围绕**联邦学习（Federated Learning, FL）中的隐私泄露问题**展开，研究以下三个核心问题：

1. 差分隐私（Differential Privacy, DP）参数（噪声标准差、裁剪范数 C）如何影响联邦学习的模型精度；
2. GRNN（Generative Regression Neural Network）梯度反演攻击能否从联邦学习传输的梯度中重建训练数据，以及攻击效果随模型训练阶段的变化规律；
3. 在联邦学习中引入差分隐私噪声能否有效阻断 GRNN 攻击，以及隐私保护与模型效用之间的折衷关系。

---

## 二、相关原理

### 2.1 联邦学习与 FedAvg 算法

#### 2.1.1 联邦学习框架

联邦学习（Federated Learning）是一种分布式机器学习范式，允许多个数据持有方（客户端）在不共享原始数据的前提下协作训练全局模型。其核心思想是"数据不出本地，模型参数共享"：每个客户端在本地数据上训练，仅将模型更新（梯度或参数差值）上传至中心服务器聚合。

形式化地，设有 $N$ 个客户端，第 $k$ 个客户端持有本地数据集 $\mathcal{D}_k$，联邦学习的目标为：

$$\min_\theta F(\theta) = \sum_{k=1}^{N} \frac{|\mathcal{D}_k|}{|\mathcal{D}|} F_k(\theta), \quad F_k(\theta) = \frac{1}{|\mathcal{D}_k|} \sum_{(x,y) \in \mathcal{D}_k} \ell(f_\theta(x), y)$$

其中 $\theta$ 为全局模型参数，$f_\theta$ 为模型，$\ell$ 为损失函数。

#### 2.1.2 FedAvg 算法

本实验采用 McMahan 等人提出的**联邦平均算法（FedAvg）**，其每轮通信流程为：

1. **下发**：服务器将当前全局模型 $\theta^{(t)}$ 发送给所有客户端；
2. **本地训练**：第 $k$ 个客户端以 $\theta^{(t)}$ 为初始值，在本地数据上执行 $E$ 轮 SGD：

$$\theta_k^{(t)} \leftarrow \theta^{(t)} - \eta \nabla F_k(\theta^{(t)})$$

3. **上传**：客户端上传更新差值 $\Delta_k = \theta_k^{(t)} - \theta^{(t)}$；
4. **聚合**：服务器对所有差值取平均，更新全局模型：

$$\theta^{(t+1)} = \theta^{(t)} + \frac{1}{N} \sum_{k=1}^{N} \Delta_k$$

FedAvg 通过多轮迭代使全局模型逐步收敛。本实验中，$N=5$ 个客户端将 MNIST 训练集做 IID 均分，每轮各训练 $E=1$ 轮（learning rate $\eta=0.01$）。

#### 2.1.3 隐私威胁

FL 的安全假设是"共享梯度即安全"，但近年研究表明，**模型梯度携带了大量关于训练数据的信息**。恶意服务器可以利用截获的梯度 $g = \nabla_\theta \ell(f_\theta(x), y)$ 反推出原始训练样本 $(x, y)$。这一类攻击统称为**梯度反演攻击（Gradient Inversion Attack）**，GRNN 即为其中的代表性方法。

### 2.2 差分隐私

#### 2.2.1 定义

差分隐私（Differential Privacy, DP）是 Dwork 等人提出的严格的隐私保护形式化定义。一个随机化机制 $\mathcal{M}$ 满足 $(\varepsilon, \delta)$-差分隐私，当且仅当对于任意相邻数据集 $D, D'$（仅差一条记录）和任意输出子集 $S$：

$$\Pr[\mathcal{M}(D) \in S] \leq e^\varepsilon \cdot \Pr[\mathcal{M}(D') \in S] + \delta$$

- **$\varepsilon$（隐私预算）**：控制隐私保护强度，$\varepsilon$ 越小保护越强。$\varepsilon \to 0$ 意味着攻击者几乎无法从输出中区分任意两条数据是否参与了计算；
- **$\delta$**：允许的小概率失败，通常取 $\delta \ll 1/n$（$n$ 为数据集大小）。

当 $\delta = 0$ 时称为纯差分隐私（$\varepsilon$-DP），否则称为近似差分隐私（$(\varepsilon,\delta)$-DP）。

#### 2.2.2 噪声机制

差分隐私通过向查询结果或模型更新中注入校准噪声来实现。设函数 $f$ 的**全局敏感度**为 $\Delta f = \max_{D, D'} \|f(D) - f(D')\|$，常用的两种机制为：

**拉普拉斯机制**（满足 $\varepsilon$-DP）：

$$\mathcal{M}(D) = f(D) + \mathrm{Lap}\!\left(\frac{\Delta f}{\varepsilon}\right)^d$$

其中 $\mathrm{Lap}(b)$ 为尺度参数 $b$ 的拉普拉斯分布，密度函数 $p(x) = \frac{1}{2b} e^{-|x|/b}$，其标准差为 $\sigma = b\sqrt{2}$。

**高斯机制**（满足 $(\varepsilon,\delta)$-DP，$\varepsilon < 1$）：

$$\mathcal{M}(D) = f(D) + \mathcal{N}\!\left(0,\, \sigma^2 I\right), \quad \sigma = \frac{\Delta f \sqrt{2\ln(1.25/\delta)}}{\varepsilon}$$

在本实验中，梯度向量的 L2 敏感度通过**梯度裁剪**控制：将每个客户端的梯度 $g$ 按 L2 范数裁剪到上界 $C$：

$$\tilde{g} = g \cdot \min\!\left(1,\, \frac{C}{\|g\|_2}\right)$$

裁剪后 $\|\tilde{g}\|_2 \leq C$，从而 $\Delta f = C$。

#### 2.2.3 组合定理

在联邦学习的多轮交互中，每轮都消耗一部分隐私预算。**简单组合定理（Sequential Composition）**指出：若 $T$ 轮中每轮满足 $\varepsilon_{\text{round}}$-DP，则整个过程满足 $\varepsilon_{\text{total}}$-DP，其中：

$$\varepsilon_{\text{total}} = T \cdot \varepsilon_{\text{round}} \quad \Longrightarrow \quad \varepsilon_{\text{round}} = \frac{\varepsilon_{\text{total}}}{T}$$

本实验取 $T=30$，给定总预算 $\varepsilon_{\text{total}}$ 后均匀分配到每轮。

> 注：简单组合定理给出的是上界估计。更紧的分析方法（如矩累积 Moments Accountant、Rényi DP 组合）可以在相同总预算下允许每轮注入更少的噪声，但本实验以简单组合定理为主。

#### 2.2.4 DP-SGD 与联邦学习的结合

将差分隐私应用于联邦学习（DP-FL），每个客户端在本地训练时，对每步 SGD 计算出的梯度执行：

1. **裁剪**（Clipping）：$\tilde{g} \leftarrow g \cdot \min(1, C/\|g\|_2)$
2. **加噪**（Noising）：$\hat{g} \leftarrow \tilde{g} + \text{noise}(\sigma)$
3. **更新**（Step）：$\theta \leftarrow \theta - \eta \hat{g}$

噪声在 `loss.backward()` 之后、`optimizer.step()` 之前注入，即 DP-SGD 风格。加噪后的梯度用于本地更新，最终上传的参数差值自然继承了差分隐私保护。

### 2.3 GRNN 梯度反演攻击

#### 2.3.1 研究背景与动机

梯度反演攻击旨在从截获的梯度中还原出训练数据。已有方法包括：

| 方法 | 类型 | 支持 Batch | 分辨率 | 损失函数 | 局限 |
|------|------|-----------|--------|----------|------|
| DLG | 判别式 | ≤8 | ≤64×64 | MSE | 不稳定，batch 大即失败 |
| iDLG | 判别式 | 仅1 | ≤64×64 | MSE | 只支持单样本 |
| IG | 判别式 | ≤100 | ≤224×224 | 余弦距离+TV | 成功率偏低 |
| **GRNN** | **生成式** | **≤256** | **≤256×256** | **MSE+WD+TV** | — |

DLG 等**判别式**方法直接在像素空间优化随机初始化的"假图像"，使其在全局模型上产生的梯度逼近真实梯度。这类方法在高维空间中容易陷入局部最优、对 batch 大小和图像分辨率敏感。

GRNN 的关键创新在于采用**生成式**方法：引入一个生成器网络 $G$，将数据还原从"高维像素拟合"转化为"生成网络参数优化"，利用生成器的结构先验（由粗到细的上采样）约束搜索空间，从而更稳定、可扩展。

#### 2.3.2 威胁模型

- **攻击者**：联邦学习中的恶意中心服务器；
- **攻击者拥有**：全局模型的结构与参数（本就由服务器下发），以及某客户端上传的真实梯度 $g$；
- **攻击者不拥有**：客户端的私有图像和标签；
- **攻击目标**：仅凭 $g$ 和全局模型，还原出用于计算 $g$ 的私有训练样本 $(x, y)$。

#### 2.3.3 生成器架构

GRNN 生成器 $G$ 接受随机向量 $v \sim \mathcal{N}(0, I)$ 作为输入，通过**两个共享输入的分支**分别生成假图像和假标签：

**图像分支**（Fake-Data Generator）：
- 全连接层将 $v$ 映射为 $\text{base\_ch} \times 4 \times 4$ 的特征图（经 GLU 激活，通道减半）；
- 经过若干上采样块（Upsampling Block），每块由"最近邻 ×2 上采样 → Conv(3×3) → BN → GLU"组成；
- 目标分辨率 32×32 需要三个上采样块：$4 \to 8 \to 16 \to 32$；
- 末端 Conv 层 + Sigmoid 输出 $[0,1]$ 范围的图像。

其中 **GLU（门控线性单元）** 是激活函数 $\mathrm{GLU}(a,b) = a \odot \sigma(b)$，将通道维切为两半后做门控，通道数减半。因此每个上采样块的 Conv 输出 $2 \times \text{out\_ch}$ 通道，GLU 后恢复为 $\text{out\_ch}$。

**标签分支**（Fake-Label Generator）：
- 单层全连接 + Softmax，输出 $K$ 维概率分布（$K=10$ 对应 MNIST 类别数）。

形式化：$(\hat{x}, \hat{y}) = G_\phi(v)$，其中 $\phi$ 为生成器可训练参数。

#### 2.3.4 损失函数

生成假数据 $(\hat{x}, \hat{y})$ 后，在冻结的全局模型 $f_\theta$ 上计算假梯度 $\hat{g} = \nabla_\theta \ell(f_\theta(\hat{x}), \hat{y})$，然后最小化：

$$\mathcal{L}_{\text{attack}} = \underbrace{\sum_{i} (g_i - \hat{g}_i)^2}_{\text{MSE（平方和）}} + \underbrace{\sqrt{\sum_{i} (g_i - \hat{g}_i)^2}}_{\text{WD（L2 范数）}} + \underbrace{\alpha \cdot \mathrm{TV}(\hat{x})}_{\text{平滑正则}}$$

- **MSE**：真假梯度向量元素差的平方和，提供主要的匹配信号；
- **WD（Wasserstein Distance）**：等价于 L2 范数，衡量梯度向量整体的几何距离，与 MSE 等权配合使用；
- **TV Loss**：总变差正则，约束生成图像的空间平滑性，抑制高频噪声。权重 $\alpha = 10^{-3}$（LeNet 设置）。

三个损失项联合端到端训练，两支生成器全部可微。

#### 2.3.5 攻击算法（Algorithm 1）

```
输入：截获的真实梯度 g，全局模型 f_θ，迭代次数 I
1: v ← 从 N(0,I) 采样，作为生成器的固定输入
2: 初始化生成器参数 ϕ
3: for i = 1 to I do
4:     (x̂, ŷ) ← G_ϕ(v)                          // 生成假图、假标签
5:     ĝ ← ∇_θ ℓ(f_θ(x̂), ŷ)  [create_graph=True] // 假梯度（保持计算图）
6:     L ← MSE(g, ĝ) + WD(g, ĝ) + α·TV(x̂)       // 攻击损失
7:     ϕ ← ϕ − η · ∂L/∂ϕ                         // 只更新生成器
8: end for
9: return (x̂, ŷ)                                 // 还原的图像和标签
```

**核心**：全局模型 $\theta$ 始终冻结不更新，被优化的只有生成器 $\phi$。

#### 2.3.6 二阶可导性约束

攻击损失 $\mathcal{L}_{\text{attack}}$ 对生成器参数 $\phi$ 的梯度，通过链式法则展开为：

$$\frac{\partial \mathcal{L}_{\text{attack}}}{\partial \phi} = \frac{\partial \mathcal{L}_{\text{attack}}}{\partial \hat{g}} \cdot \underbrace{\frac{\partial \hat{g}}{\partial \hat{x}}}_{\text{混合二阶导}} \cdot \frac{\partial \hat{x}}{\partial \phi}$$

其中 $\frac{\partial \hat{g}}{\partial \hat{x}} = \frac{\partial^2 \ell}{\partial \theta \, \partial \hat{x}}$ 是关于模型参数和输入的**混合二阶偏导数**。这要求：

1. 计算假梯度时必须保持计算图（PyTorch 中 `create_graph=True`）；
2. 模型的激活函数必须**处处二阶可导**。

ReLU 在 $x=0$ 处不可导，二阶导数几乎处处为零，梯度信号无法有效回传。因此 GRNN 攻击必须使用 **Sigmoid** 激活函数（$\sigma(x) = 1/(1+e^{-x})$，处处光滑）。而 FL 训练实验使用 ReLU 以获得更好的收敛性能。

#### 2.3.7 PSNR 评价指标

攻击还原质量用**峰值信噪比（PSNR）**衡量：

$$\mathrm{PSNR} = 10 \cdot \log_{10}\!\left(\frac{\mathrm{MAX}^2}{\mathrm{MSE}}\right)$$

其中 $\mathrm{MAX}=1$（像素值范围 $[0,1]$），$\mathrm{MSE}$ 为还原图与原图的均方误差。PSNR 越高表示越相似：

| PSNR 范围 | 含义 |
|:---------:|------|
| > 40 dB | 人眼无法区分，几乎完全一致 |
| 30~40 dB | 高质量还原，数字可辨识 |
| 20~30 dB | 中等质量，轮廓可见但细节模糊 |
| < 10 dB | 还原失败，与随机噪声无异 |

### 2.4 实验整体设计

#### 2.4.1 实验流程

```
步骤一：FedAvg 基线（无 DP）       → 验证 FL 可正常收敛，获得精度上界
步骤二：DP 参数扫描                → 分析噪声强度对 FL 精度的影响
步骤三：GRNN 攻击基线              → 验证攻击成功，量化 PSNR
步骤四：训练阶段 vs 攻击效果       → 分析 FL 不同训练阶段的安全风险
步骤五：DP 防御 GRNN               → 量化 DP 噪声对攻击的阻断效果
```

#### 2.4.2 参数对齐说明

| 实验 | 模型激活 | 原因 |
|------|----------|------|
| 步骤一/二（FL 精度） | **ReLU** | 收敛更快、精度更高；Sigmoid 在噪声扰动下容易饱和 |
| 步骤三/四/五（GRNN） | **Sigmoid** | GRNN 二阶反传要求激活函数处处二阶可导 |

防御实验（步骤五）中，对单步梯度直接注入指定标准差的物理噪声（拉普拉斯/高斯），再作为 GRNN 攻击的目标梯度，模拟攻击者从带噪更新中尝试反演的场景。

---

## 三、实验环境与参数

| 参数 | 值 |
|------|-----|
| 数据集 | MNIST（训练集 60,000，测试集 10,000），padding 至 32×32 |
| 模型 | LeNet-5（MaxPool，6→16→120→84→10） |
| 客户端数 N | 5 |
| 通信轮数 T | 30 |
| 本地训练轮 E | 1 |
| 本地学习率 lr | 0.01 |
| 本地 batch size | 64（FL 训练）/ 1（GRNN 攻击） |
| 噪声标准差扫描范围 | {10⁻¹, 10⁻², 10⁻³, 10⁻⁴, 0} |
| GRNN 优化器 | RMSprop（lr=1×10⁻⁴, momentum=0.99） |
| GRNN 迭代次数 | 2,000 |
| TV 正则权重 α | 1×10⁻³ |

---

## 四、实验结果与分析

### 4.1 联邦学习基线（无 DP）

标准 FedAvg（ReLU LeNet，5 客户端 IID 切分，无任何噪声）在 30 轮通信后测试精度达到 **97.38%**（无噪声基线），验证了实验框架的正确性，并为后续 DP 实验提供对照基线。

**终端输出（30 轮精度日志）：**

[image: 终端截图 — run_fl.sh 输出的 30 轮精度日志，含最终精度]

**精度 vs 通信轮数曲线：**

[image: results/baseline_acc.png]

从收敛曲线可观察到：
- **前 5 轮**快速上升至 ~80%，模型学会了基本的数字特征；
- **5~15 轮**稳步爬升至 ~95%，进入精细调优阶段；
- **15~30 轮**缓慢趋近 ~97%，收敛至平衡点。

这一收敛特性符合 FedAvg 在 IID 数据分布下的典型行为：数据分布一致使得各客户端的本地更新方向大体一致，聚合后的全局梯度方向准确，收敛快速。

### 4.2 DP 噪声对 FL 性能的影响

**简单组合定理换算表（终端输出）：**

[image: 终端截图 — run_dpfl.sh 输出的组合定理换算表，将 noise_std 换算为对应 ε]

#### 4.2.1 不同噪声标准差的影响

本实验直接控制注入梯度的物理噪声标准差（noise_std），扫描 $\{10^{-1}, 10^{-2}, 10^{-3}, 10^{-4}, 0\}$ 五个水平，分别在拉普拉斯和高斯机制下观察对 FL 精度的影响：

| noise_std | Laplace 最终精度 | Gaussian 最终精度 |
|:---------:|:----------------:|:-----------------:|
| 0.1 | 97.26% | 97.76% |
| 0.01 | 97.45% | 97.31% |
| 0.001 | 97.14% | 97.59% |
| 0.0001 | 97.59% | 97.53% |
| 0（无噪声） | 97.38% | 97.62% |

**分析**：

1. **噪声影响极小**：即使最大噪声 noise_std=0.1，精度仍维持在 97% 以上，最大降幅不超过 0.5%。这一结论在两种机制下一致。

2. **原因解释**：LeNet-5 的参数维度 $d=61{,}706$。在 FedAvg 中，$N=5$ 个客户端的更新被平均，噪声的方差被缩减为原来的 $1/N$。而每个参数维度上的噪声是独立的，高维聚合效应使得噪声的整体影响被稀释。具体地，噪声对单次更新的信噪比为：

$$\mathrm{SNR} = \frac{\|g\|_2}{\sigma \sqrt{d}} \approx \frac{4.35}{0.1 \times \sqrt{61706}} \approx 0.175$$

即使逐维 SNR 较低，FedAvg 的多轮迭代和多客户端平均效应使得模型仍能沿正确方向收敛。

3. **收敛速度差异**：从逐轮数据中可观察到，noise_std=0.1 时前 5 轮精度约 65~82%，而无噪声时前 5 轮约 50~77%（随机波动范围内）。这说明在本实验的参数范围内，噪声对收敛速度的影响甚至不构成可观测的延迟。

4. **两种机制对比**：拉普拉斯与高斯在相同 noise_std 下表现接近，精度差异在 0.5% 以内（随机波动范围）。理论上高斯噪声的概率密度更集中（轻尾），对极端值的影响更小，但在本实验的参数范围内这一差异不显著。

**最终精度汇总表（终端输出）：**

[image: 终端截图 — run_dpfl.sh 输出的最终精度汇总表]

**拉普拉斯噪声 — 不同 noise_std 精度曲线：**

[image: results/dp_laplace_acc.png]

**高斯噪声 — 不同 noise_std 精度曲线：**

[image: results/dp_gaussian_acc.png]

### 4.3 GRNN 梯度反演攻击

#### 4.3.1 攻击基线（无 DP）

在随机初始化的 Sigmoid LeNet 上，对单张训练图像（batch_size=1）计算真实梯度后进行 GRNN 攻击（2,000 次迭代，RMSprop，lr=10⁻⁴，momentum=0.99）：

| 指标 | 数值 |
|------|------|
| 真实标签 | 6（数字"6"） |
| 梯度向量维度 | 61,706 |
| 初始损失（total） | ~18.33 |
| 最终损失（total） | 4.62×10⁻⁴ |
| 损失下降倍率 | ~40,000× |
| **平均 PSNR** | **40.50 dB** |

**终端输出（损失下降过程 + PSNR）：**

[image: 终端截图 — run_attack.sh 输出的迭代损失日志（前几行+最后几行）及最终 PSNR]

**分析**：

1. **攻击完全成功**：PSNR > 40 dB 表明还原图像与原图几乎完全一致，人眼无法区分差异。这验证了 GRNN 论文的核心结论——在联邦学习中，梯度泄露可导致训练数据的完整还原。

2. **优化收敛性**：损失从 18.33 下降至 $4.62 \times 10^{-4}$，下降约 4 个数量级。从损失曲线可观察到：前 300 次迭代快速下降（MSE 主导），300~1000 次进入精细调整阶段（WD 主导），1000 次后趋于平坦。

**GRNN 还原过程（由粗到细）：**

[image: results/attack/recover_process.png]

3. **生成式方法的优势**：与判别式方法（DLG）相比，GRNN 利用生成器的上采样结构提供了"由粗到细"的图像先验——iter=0 时为均匀灰色，iter=100 时出现大致轮廓，iter=300 时数字形状清晰，iter=1000 后细节逐步完善。

**原图 vs 还原图对比（含 PSNR 标注）：**

[image: results/attack/compare.png]

**MSE / WD 损失收敛曲线：**

[image: results/attack/loss_curve.png]

#### 4.3.2 训练阶段对攻击效果的影响

为研究 GRNN 攻击在 FL 训练不同阶段的有效性，对同一张图像（数字"6"），在全局模型经过不同预训练步数后分别发起攻击：

| 预训练步数 | 测试精度 | 梯度范数 $\|g\|_2$ | PSNR (dB) | 攻击效果 |
|:---:|:---:|:---:|:---:|:---:|
| 0（随机初始化） | 9.58% | 4.31 | 39.53 | 完全成功 |
| 1 | 11.35% | 3.98 | 35.63 | 成功 |
| 3 | 11.35% | 3.05 | 40.98 | 完全成功 |
| 5 | 11.35% | 2.34 | 38.45 | 完全成功 |
| 10 | 11.35% | 1.57 | 40.25 | 完全成功 |
| 20 | 11.35% | 1.22 | 25.78 | 部分成功 |
| 30 | 54.37% | 9.23 | 27.16 | 部分成功 |
| 50 | 97.63% | 0.04 | 24.06 | 质量下降 |

**分析**：

1. **训练早期（0~10 步）攻击最为有效**：PSNR 稳定在 35~41 dB，还原质量极高。此时模型远未收敛，权重接近随机初始化，梯度向量在参数空间中各方向的分量均衡，携带了丰富的输入图像信息。这与 GRNN 论文的结论一致："未收敛模型比收敛模型更易攻击"。

2. **训练中期（20~30 步）攻击仍有效但质量下降**：PSNR 约 25~27 dB，数字轮廓可辨识但细节模糊。模型开始学到有意义的特征，梯度逐步集中到与分类相关的方向上，对输入的敏感性降低。

3. **训练末期（50 步，精度 97.63%）**：PSNR 降至 24 dB，梯度范数骤降至 0.04（模型接近收敛，损失梯度极小），可用的梯度信息大幅减少，但仍高于"完全失败"的阈值（~3 dB）。

4. **梯度范数与攻击效果的关系**：梯度范数并非攻击效果的唯一决定因素——第 30 步时梯度范数高达 9.23（模型精度跳变至 54%，学到了关键特征，梯度方向更集中），但 PSNR 仅 27 dB。真正决定还原难度的是梯度向量中**与输入图像相关的信息含量**，而非其绝对大小。

5. **对 FL 安全的启示**：GRNN 攻击威胁贯穿训练全程，尤其在**训练早期阶段最为危险**——此时每个客户端的梯度包含最多的原始数据信息，而模型尚未收敛意味着攻击机会多（每轮都有新的可攻击梯度）。

**各训练阶段还原图对比：**

[image: results/train_stage/recovered.png]

**训练阶段 vs 梯度范数 & PSNR 双轴曲线：**

[image: results/train_stage/stage_curve.png]

### 4.4 差分隐私防御效果

#### 4.4.1 实验设计

对同一张图像的单样本梯度（与 4.3 节相同），直接注入指定标准差的物理噪声（拉普拉斯或高斯），得到带噪梯度 $\tilde{g} = g + \text{noise}(\sigma)$，再以 $\tilde{g}$ 为目标运行 GRNN 攻击 2000 次迭代。

噪声标准差扫描范围：$\{10^{-1}, 10^{-2}, 10^{-3}, 10^{-4}, 0\}$。

#### 4.4.2 PSNR 结果

| noise_std | Laplace PSNR (dB) | Gaussian PSNR (dB) | 相对无噪声降幅 |
|:---------:|:------------------:|:-------------------:|:--------------:|
| 0（无噪声） | 39.17 | 39.76 | — |
| 10⁻⁴ | 17.05 | 16.94 | −22 dB |
| 10⁻³ | 7.74 | 7.49 | −32 dB |
| 10⁻² | 3.46 | 3.92 | −36 dB |
| 10⁻¹ | 3.00 | 3.53 | −36 dB |

**拉普拉斯防御效果对比大图（各 noise_std 下还原图 vs 真实图）：**

[image: results/defense/defense_laplace.png]

**高斯防御效果对比大图：**

[image: results/defense/defense_gaussian.png]

**PSNR 柱状图（两种机制对比）：**

[image: results/defense/psnr_bar.png]

#### 4.4.3 逐层分析

**无噪声基线（noise_std=0）**：
- PSNR ~39 dB，与 4.3 节一致，攻击完全成功。

**极小噪声（noise_std=10⁻⁴）**：
- PSNR 降至 ~17 dB，还原图像已出现明显退化：数字的大致轮廓仍可辨识，但边缘模糊、出现噪点。
- 噪声 L2 范数：$\sigma \sqrt{d} = 10^{-4} \times \sqrt{61706} \approx 24.84$，约为真实梯度范数（4.31）的 5.8 倍。
- 尽管噪声已远超信号，GRNN 的生成器仍能"猜"出部分低频结构——这体现了生成式方法的鲁棒性。

**小噪声（noise_std=10⁻³）**：
- PSNR 降至 ~7.5 dB，还原图像几乎全为噪声，仅有极微弱的结构痕迹。
- 噪声 L2 范数 ~248，约为梯度范数的 57 倍。攻击实质性失败。

**中等/大噪声（noise_std=10⁻² ~ 10⁻¹）**：
- PSNR 降至 3~4 dB，与随机噪声图像的 PSNR（理论极限约 0 dB）接近。
- GRNN 完全无法从带噪梯度中提取任何有效信息，还原图像为纯噪声。

#### 4.4.4 防御的"相变"特性

从数据中可以观察到，PSNR 随 noise_std 的变化呈现明显的**相变（phase transition）**行为：

- noise_std 从 0 增大到 $10^{-4}$ 时，PSNR 从 39 dB 骤降到 17 dB（降幅 22 dB）；
- noise_std 从 $10^{-4}$ 继续增大时，PSNR 进一步下降但幅度递减（从 17→7.5→3.5→3）。

这说明存在一个**临界噪声阈值**（约 noise_std ∈ [10⁻⁴, 10⁻³]），低于此阈值时攻击仍有一定效果，超过后攻击彻底失败。物理解释为：

$$\text{SNR}_{\text{grad}} = \frac{\|g\|_2}{\sigma \sqrt{d}} = \frac{4.31}{\sigma \times 248.4}$$

- 当 $\sigma = 10^{-4}$ 时，$\text{SNR}_{\text{grad}} \approx 0.17$（信号已被淹没，但生成器的结构先验仍可部分补偿）；
- 当 $\sigma = 10^{-3}$ 时，$\text{SNR}_{\text{grad}} \approx 0.017$（完全无法恢复）。

#### 4.4.5 两种噪声机制的对比

| noise_std | Laplace PSNR | Gaussian PSNR | 差值 |
|:---------:|:------------:|:-------------:|:----:|
| 10⁻⁴ | 17.05 | 16.94 | −0.11 |
| 10⁻³ | 7.74 | 7.49 | −0.25 |
| 10⁻² | 3.46 | 3.92 | +0.46 |
| 10⁻¹ | 3.00 | 3.53 | +0.53 |

在相同标准差下，两种机制的防御效果几乎一致（差异 < 0.5 dB）。理论上：
- 拉普拉斯分布为重尾分布，偶尔会产生极大噪声值，对少数参数维度造成强烈扰动；
- 高斯分布为轻尾分布，噪声更均匀地分布在各维度。

在本实验中两者差异不显著，说明 GRNN 攻击对噪声的"形状"不敏感，决定防御效果的主要是噪声的**绝对量级**（标准差）。

#### 4.4.6 隐私-效用折衷分析

**Trade-off 总结表（终端输出）：**

[image: 终端截图 — run_defense.sh 输出的 Trade-off 总结表（noise_std × 机制 × PSNR）]

综合 FL 精度（4.2 节）与防御 PSNR（4.4.2 节），绘制完整的隐私-效用图景：

| noise_std | FL 精度（Laplace） | GRNN PSNR（Laplace） | 综合评价 |
|:---------:|:------------------:|:--------------------:|:--------:|
| 0 | 97.38% | 39.17 dB | 无保护，隐私完全暴露 |
| 10⁻⁴ | 97.59% | 17.05 dB | 精度无损，攻击大幅退化但未完全失效 |
| 10⁻³ | 97.14% | 7.74 dB | 精度无损（−0.2%），攻击实质失败 |
| 10⁻² | 97.45% | 3.46 dB | 精度无损，攻击完全失败 |
| 10⁻¹ | 97.26% | 3.00 dB | 精度无损（−0.1%），攻击完全失败 |

**核心发现**：

1. **"免费午餐"区间存在**：在 noise_std ∈ [10⁻³, 10⁻¹] 范围内，FL 精度几乎不受影响（降幅 < 0.5%），而 GRNN 攻击完全失败（PSNR < 8 dB）。这意味着在本实验设置（LeNet + MNIST + 5 客户端）下，存在一个参数区间可以**同时实现模型效用与隐私保护**。

2. **高维度是天然盟友**：模型参数空间的高维度（$d=61,706$）使得：
   - 对 FL 训练：每步更新是对高维噪声的单次采样，FedAvg 的多轮迭代和多客户端平均有效消除了噪声的影响；
   - 对 GRNN 攻击：噪声在高维空间中以 $\sqrt{d}$ 量级累积，即使每维噪声很小，整体也足以破坏梯度-数据的对应关系。

3. **防御代价极低**：与 GRNN 论文的预期不同（论文指出 GRNN 对噪声有较强鲁棒性），本实验发现在单样本梯度场景下，**极小的噪声即可破坏攻击**。这是因为高维空间中的噪声累积效应远超直觉——noise_std=10⁻³ 对应的梯度空间 SNR 仅为 0.017，远低于 GRNN 生成器的学习能力下限。

---

## 五、结论

### 5.1 核心发现

1. **DP 噪声对 FL 精度的影响可忽略**：在 noise_std ∈ [10⁻⁴, 10⁻¹] 的全范围内，拉普拉斯和高斯两种机制对 LeNet-MNIST 联邦学习精度的影响均小于 0.5%（最终精度稳定在 97% 以上），可实现近似无损的隐私保护。高维参数空间的平均效应和 FedAvg 多轮迭代是精度鲁棒性的关键。

2. **GRNN 攻击在无保护时极其危险**：在无 DP 保护的联邦学习中，GRNN 可以约 40 dB 的 PSNR 从单样本梯度精确还原训练图像，人眼无法区分还原图与原图。攻击效果在训练早期最强（PSNR 35~41 dB），训练末期有所下降（PSNR ~24 dB）但仍远高于失败阈值，威胁贯穿 FL 训练全程。

3. **DP 防御呈现"相变"特性**：存在一个临界噪声阈值（noise_std ≈ 10⁻⁴~10⁻³），低于此阈值攻击仍有部分效果（PSNR ~17 dB），超过后攻击彻底崩溃（PSNR < 8 dB，趋近随机噪声水平）。这一相变行为源于高维参数空间中噪声以 $\sqrt{d}$ 量级累积的效应。

4. **"免费午餐"区间**：在 noise_std ∈ [10⁻³, 10⁻¹] 范围内，FL 精度几乎无损（降幅 < 0.5%）且 GRNN 攻击完全失败（PSNR < 8 dB）。这一区间的存在说明，在本实验设置（LeNet + MNIST + 5 客户端）下，可以**同时实现模型效用与隐私保护的双赢**。

5. **两种噪声机制无显著差异**：在相同标准差下，拉普拉斯与高斯机制的防御效果（PSNR 差异 < 0.5 dB）和对 FL 精度的影响（差异 < 0.5%）均无统计显著差异，说明噪声的绝对量级比分布形状更重要。

### 5.2 与 GRNN 论文结论的对比

| 论文结论 | 本实验验证 |
|---------|----------|
| 未收敛模型比收敛模型更易攻击 | **验证**：随机初始化 PSNR=39.5 dB，训练 50 步后降至 24 dB |
| GRNN 对噪声有较强鲁棒性 | **部分验证**：noise_std=10⁻⁴ 时 PSNR 仍有 17 dB（优于 DLG 等方法），但 noise_std ≥ 10⁻³ 时完全失效 |
| 生成式方法优于判别式方法 | **间接验证**：还原过程呈现"由粗到细"的渐进改善，与论文描述一致 |

### 5.3 局限性

1. **激活函数不统一**：FL 训练使用 ReLU（收敛性更好），GRNN 攻击使用 Sigmoid（二阶可导性要求）。两者的精度与 PSNR 结果不能在同一模型上直接对比。理想情况下应使用同一激活函数贯穿所有实验，但这受限于 Sigmoid-FL 收敛困难和 ReLU 不支持 GRNN 二阶反传的技术约束。

2. **数据集与模型规模有限**：MNIST（28×28 灰度手写数字）和 LeNet-5（61,706 参数）均为小规模场景。在更复杂的数据集（CIFAR-100、ImageNet）和更大的模型（ResNet、ViT）上，攻击与防御的动态可能不同：更大的模型意味着更高维的梯度空间，可能使噪声累积效应更强；但更复杂的图像也意味着还原难度本身增大。

3. **单样本攻击场景**：本实验仅测试了 batch_size=1 的场景。GRNN 论文表明其支持 batch≤256，但随 batch 增大还原质量下降。实际 FL 中客户端通常使用较大 batch（如 64），多样本梯度的聚合本身也提供了一定程度的隐私保护。

4. **组合定理精度**：本实验使用简单组合定理（$\varepsilon_{\text{total}} = T \cdot \varepsilon_{\text{round}}$）进行预算分配。更紧的分析方法（如 Rényi DP 组合、Moments Accountant）可以在相同总预算下允许每轮注入更少的噪声，从而可能进一步缩小"免费午餐"区间。

5. **IID 数据分布假设**：本实验将 MNIST 训练集 IID 均分给 5 个客户端。实际联邦学习场景中数据通常是 Non-IID 分布，此时 FedAvg 收敛更慢，DP 噪声对精度的影响可能更大。

### 5.4 未来工作方向

1. **更紧的隐私预算分析**：采用 Moments Accountant 或 Rényi DP 替代简单组合定理，研究更紧的预算分配下"免费午餐"区间是否仍然存在。

2. **Non-IID 场景**：在 Dirichlet 分布等 Non-IID 数据切分下，研究 DP 噪声对 FL 收敛的影响是否更显著。

3. **安全聚合**：研究安全聚合（Secure Aggregation）与 DP 的组合防御效果——安全聚合使服务器只能看到聚合后的梯度，进一步增大攻击难度。

4. **自适应攻击**：研究攻击者是否可以利用多轮梯度的相关性来消除 DP 噪声的部分效果。

---

## 附：产出文件清单

| 文件 | 内容 |
|------|------|
| `results/baseline_acc.png` | FL 基线精度曲线（无 DP，30 轮） |
| `results/dp_laplace_acc.png` | 不同 noise_std 下拉普拉斯噪声精度曲线 |
| `results/dp_gaussian_acc.png` | 不同 noise_std 下高斯噪声精度曲线 |
| `results/dp_acc_data.csv` | FL 精度原始数据 |
| `results/attack/recover_process.png` | GRNN 还原过程（逐步可视化） |
| `results/attack/compare.png` | 原图 vs 还原图对比（含 PSNR） |
| `results/attack/loss_curve.png` | GRNN 损失收敛曲线 |
| `results/train_stage/stage_curve.png` | 训练阶段 vs 梯度范数 & PSNR 双轴图 |
| `results/train_stage/recovered.png` | 各训练阶段还原图对比 |
| `results/defense/defense_laplace.png` | 拉普拉斯 DP 防御效果对比大图 |
| `results/defense/defense_gaussian.png` | 高斯 DP 防御效果对比大图 |
| `results/defense/psnr_bar.png` | 防御效果 PSNR 柱状图 |
| `results/defense/psnr_data.csv` | 防御 PSNR 原始数据 |
